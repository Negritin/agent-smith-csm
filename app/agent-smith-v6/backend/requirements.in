# =============================================================================
# requirements.in — human-edited dependency ranges (source of truth)
# =============================================================================
# Edit THIS file, then recompile the fully-pinned, hash-locked requirements.txt.
# Run from the REPO ROOT so the generated paths match the CI lock-drift gate:
#
#   uv pip compile backend/requirements.in -o backend/requirements.txt \
#       --generate-hashes --python-version 3.11 --no-header
#
# (or, with pip-tools: pip-compile --generate-hashes requirements.in)
#
# The compiled requirements.txt is what Docker installs with --require-hashes.
# Do NOT hand-edit requirements.txt.
# =============================================================================

# FastAPI Core
fastapi>=0.109.0
# uvicorn 0.27 -> 0.34: exigido pelo SDK mcp (>=0.31.1); decisao explicita 2026-06-12.
uvicorn[standard]==0.34.0
pydantic==2.12.4
pydantic-settings==2.12.0

# LangChain & LangGraph (versões do ambiente local — hard-pinned)
langchain>=1.3.4,<2
langchain-anthropic>=1.1.0,<2
langchain-community>=0.4.2,<1
langchain-core>=1.4,<2
langchain-experimental>=0.4.0,<1
langchain-google-genai>=3.1.0,<4
langchain-openai>=1.1.14,<2
langchain-text-splitters>=1.1.2,<2
langgraph>=1.2.4,<2
langgraph-checkpoint>=4.1.1,<5
langgraph-prebuilt>=1.1.0,<1.2
langgraph-sdk>=0.4,<0.5

# LLM Providers
openai>=1.10.0
anthropic>=0.18.0
google-ai-generativelanguage==0.9.0

# Cryptography
cryptography>=42.0.0
bcrypt>=4.0.0

# Database & Storage
supabase==2.10.0
psycopg[binary]>=3.0.0
psycopg-pool>=3.2.0
langgraph-checkpoint-postgres>=3.1.0

# Vector Store
qdrant-client>=1.11.0

# Object Storage
minio==7.2.3

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0

# MCP remoto (SDK oficial Python — streamablehttp_client + ClientSession)
# httpx exigido pelo SDK (>=0.27.1,<1.0) e compativel com httpx==0.27.2 pinado.
# Exige uvicorn>=0.31.1 — pin do uvicorn subido para 0.34.0 acima (2026-06-12).
mcp>=1.10

# Utils
httpx>=0.26
requests>=2.31.0
python-dotenv>=1.2.2
python-multipart>=0.0.18,<0.1  # CVE-2024-53981 (DoS via malformed multipart) — patched in 0.0.18
pytz>=2024.1

# Evaluation
pandas>=2.0.0
tqdm>=4.66.0

# Reranking
cohere>=5.0.0

# Hybrid Search
fastembed>=0.2.0

# Web Search
tavily-python>=0.3.0

# Message Buffer
redis>=5.0.1
apscheduler>=3.10.4
python-slugify>=8.0.0

# Background Workers (Billing)
celery[redis]>=5.3.0

# Retry Logic
tenacity>=8.2.0

# Observability (LangSmith)
langsmith>=0.1.0

# Cache Management
cachetools>=5.3.0

# Rate Limiting
slowapi==0.1.9

# Error Tracking (Sentry) — pin a lower bound (was unversioned)
sentry-sdk[fastapi]>=2.0.0

# Payments
stripe>=7.0.0

# Email
sendgrid>=6.10.0

# PII & Safety
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.0.0
groq>=0.4.0

# Server
gunicorn>=21.2.0

# Code Quality
ruff>=0.14.0
